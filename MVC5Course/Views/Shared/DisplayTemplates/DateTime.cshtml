@model  DateTime?

@{ 
    var htmlAttributes = new
    {
        @class = "form-control text-box single-line valid",
        type = "date"
    };

}

    @if (Model.HasValue)
    {
         @Model.Value.ToString("yyyy-MM-dd")
    }

